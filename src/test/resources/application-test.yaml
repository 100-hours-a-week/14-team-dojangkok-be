# application-test.yml
spring:
  application:
    name: dojangkok-backend

  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  data:
    redis:
      host: localhost
      port: 6379

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: test
            client-secret: test
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile_image
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
app:
  oauth2:
    success-redirect: http://localhost:3000/auth/success

jwt:
  issuer: dojangkok
  secret: il4ZcK5KwIiCOhHs6xkkBrtOwTjQqh3YK2OpQWO0GgfBgMJqpPQY6Kw4RcwaAajmKUMQB8u7GZHHXfOo5IS9qg==
  access-exp-min: 30
  refresh-exp-day: 14

ai:
  service:
    url: http://localhost:8000
    token: test
    mock-enabled: true

internal:
  token: test

cloud:
  aws:
    s3:
      bucket: test-bucket
    region:
      static: ap-northeast-2
    credentials:
      access-key: test
      secret-key: test
    stack:
      auto: false